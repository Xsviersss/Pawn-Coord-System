# Pawn-Coord-System
A simple SA-MP Pawn script containing a /coord command for teleporting players to specific coordinates. The command supports X, Y, and Z position input using sscanf2. Includes an optional admin permission check system that can be customized or removed depending on your server setup.
